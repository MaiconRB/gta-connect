version: 2

secret:
  ignored-paths:
    # Hashes de integridade (SRI) do npm, não são segredos — falso positivo comum
    # de detectores de "high entropy secret" em qualquer projeto Node.
    - "frontend/package-lock.json"
